[mod-setting-name]
space-connection-signals-only-direct-connections=Only generate direct starmap connections
space-connection-signals-one-per-pair=Generate one signal per pair (galaxy order)

[mod-setting-description]
space-connection-signals-only-direct-connections=Only generate signals for planet/location pairs that have a direct space-connection on the starmap.
space-connection-signals-one-per-pair=Generate only one signal per pair of locations. The direction is chosen by galaxy order (earlier → later).